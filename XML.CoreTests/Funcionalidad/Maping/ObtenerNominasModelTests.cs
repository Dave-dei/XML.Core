using Microsoft.VisualStudio.TestTools.UnitTesting;

using System.IO;
using System.Xml.Linq;

namespace XML.Core.Funcionalidad.Maping.Tests
{
    [TestClass()]
    public class ObtenerNominasModelTests
    {
        [TestMethod()]
        public void InstanciarTest()
        {
            Assert.IsTrue(true);

            //string archivo = Path.Combine(Directory.GetCurrentDirectory(), "00100001201909300000621.xml");
            //ObtenerNominasModel model = new ObtenerNominasModel(XDocument.Load(archivo));
            
            //var resultado = model.Mapear();
         
            //Assert.IsTrue(resultado.Error == null);
        }
    }
}